/*
  Image source map for the deployable version.

  Keep these keys stable and replace only the values when you want to swap art.
  Local files work out of the box on GitHub Pages. External URLs also work for
  normal <img>/<svg image> rendering; the WebGL refraction layer additionally
  requires the remote server to allow CORS.
*/
window.SITE_ASSETS = {
  mermaid: "assets/mermaid.webp",
  catChair: "assets/cat-chair.webp",
  chroma: "assets/chroma.webp",
};
